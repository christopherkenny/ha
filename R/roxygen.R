template_var_ip <- function() { # nocov start
  'IP address. Default is `NULL`, which uses the local host.'
} # nocov end

template_var_timestamp <- function() { # nocov start
  'Time in `YYYY-MM-DDThh:mm:ssTZD` format.'
} # nocov end


template_var_entity_id <- function() { # nocov start
  'Entity ID. For example, `sensor.temperature`.'
} # nocov end

template_var_states <- function() { # nocov start
  'State object. For example, `on` or `below_horizon`.'
} # nocov end

template_var_state <- function() { # nocov start
  'A state value. For example, `on` or `off`.'
} # nocov end

template_var_domain <- function() { # nocov start
  'An entity domain. For example, `light` or `switch`.'
} # nocov end

template_var_service <- function() { # nocov start
  'A service name. For example, `turn_on` or `turn_off`.'
} # nocov end
